{% extends 'base.html.twig' %}

{% block title %}Edit Event{% endblock %}

{% block body %}

{{ include('event/_form.html.twig', {'button_label': 'Update'}) }}

<div class="bg-light text-center rounded p-4" style="background-color: #000000;">

    {{ include('event/_delete_form.html.twig') }}
    <a class="btn btn-sm btn-warning" href="{{ path('app_event_index') }}">back to list</a>

    <style>
        .bg-light {
            background-color: transparent !important; /* Annuler la couleur de fond de la classe bg-light */
        }
    </style>

</div>

{% endblock %}
